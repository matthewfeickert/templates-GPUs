name: Apptainer Images

on:
  # push:
    # branches:
    #   - main
    # tags:
    #   - 'v*'
    # paths:
    #   - 'containers/pixi/pixi.toml'
    #   - 'containers/pixi/pixi.lock'
    #   - 'containers/pixi/apptainer.def'
    #   - 'containers/pixi/app/**'
  pull_request:
    paths:
      - 'containers/pixi/pixi.toml'
      - 'containers/pixi/pixi.lock'
      - 'containers/pixi/apptainer.def'
      - 'containers/pixi/app/**'
  release:
    types: [published]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions: {}

jobs:
  apptainer:
    name: Build and publish images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Free disk space
        uses: AdityaGarg8/remove-unwanted-software@v5
        with:
          remove-android: 'true'
          remove-dotnet: 'true'
          remove-haskell: 'true'

      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Get commit SHA
        id: meta
        run: |
            # Get the short commit SHA (first 7 characters)
            SHA=$(git rev-parse --short HEAD)
            echo "sha=sha-$SHA" >> $GITHUB_OUTPUT

      - name: Install Apptainer
        uses: eWaterCycle/setup-apptainer@v2

      - name: Build container from definition file
        working-directory: ./containers/pixi
        run: apptainer build gpu-noble-cuda-12.9.sif apptainer.def

      - name: Test container
        working-directory: ./containers/pixi
        run: apptainer test gpu-noble-cuda-12.9.sif

      - name: Login to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy built container
        if: github.event_name != 'pull_request'
        working-directory: ./containers/pixi
        run: |
            apptainer push gpu-noble-cuda-12.9.sif oras://ghcr.io/matthewfeickert/templates-gpus:apptainer-mnist-gpu-noble-cuda-12.9
            # apptainer push gpu-noble-cuda-12.9.sif oras://ghcr.io/matthewfeickert/templates-gpus:apptainer-mnist-gpu-noble-cuda-12.9-${{ steps.meta.outputs.sha }}
